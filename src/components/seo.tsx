import React from "react"
import { Helmet } from "react-helmet"
import { useStaticQuery, graphql } from "gatsby"

const Seo = ()=>{
    return (
        <Helmet title='wadim on the internet'>
        </Helmet>
    )
}

export default Seo